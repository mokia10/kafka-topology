---
context: "context"
origin: "origin"
projects:
  - name: "projectB"
    topics:
      - name: "foo"
        config:
          replication.factor: "3"
          num.partitions: "3"
      - name: "bar"
        dataType: "avro"
        config:
          replication.factor: "3"
          num.partitions: "3"
